---
apiVersion: v1
kind: Namespace
metadata:
  name: pipeline-demo

---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: myweb2
  name: myweb2
  namespace: pipeline-demo
spec:
  replicas: 1
  selector:
    matchLabels:
      app: myweb2
  template:
    metadata:
      labels:
        app: myweb2
    spec:
      containers:
      - image: justmeandopensource/myweb:<TAG>
        imagePullPolicy: Always
        name: myweb2

---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: myweb2
  name: myweb2
  namespace: pipeline-demo
spec:
  ports:
  - nodePort: 32223
    port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: myweb2
  type: NodePort
